<%= turbo_frame_tag category do %>
  <tr>
    <td><%= link_to task.title, family_task_path(family, task), class: 'text-decoration-none link-dark wide-link' %></td>
    <td><%= link_to task.points, family_task_path(family, task), class: 'text-decoration-none link-dark wide-link' %></td>
  </tr>
<% end %>