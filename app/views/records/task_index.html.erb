<!--そのカテゴリに属するタスク一覧のページだよ。こっちはタスク記録画面へ飛ぶよ-->
<div class="container-fluid">
  <div class="center-block">
    <!--コメントを残しとかないとfなんて知らないよってエラー出るから残すよ-->
    <!--<%= form_with model: @task_user, url: family_records_path(@family) do |f| %>-->
      <%= turbo_frame_tag @category do %>
        <%= link_to new_family_record_path(@family), class: "text-decoration-none text-body" do %>
          <div class="row bg-secondary-subtle py-2 mt-2 mx-auto rounded border-bottom border-secondary">
            <div class="col-1">▼</div>
            <div class="col-11"><%= @category.name %></div>
          </div>
        <% end %>
        <% @category.tasks.each do |task| %>
          <%= render partial: 'task_for_record', locals: { task: task, record: @record } %>
        <% end %>
      <% end %>
    <!--<% end %>-->
  </div>
</div>