<!--記録するタスクの詳細を表示してカウント数を入力・記録したいページ-->
<div class="container-fluid">
  <%= render partial: 'shared/show_task', locals: { task: @task } %>
  <div class="center-block">
    <div class="text-ceenter">
      <!--<%= form_with model: @task, url: family_records_path, local: true do |f| %>
        <%= f.label :count, 'カウント：' %>
        <%= f.number_field :count %>
        <%= f.select :count, {'回': 0, '時間': 1}, { selected: 0 } %><br>
        <%= f.submit '記録' %>
      <% end %>-->
      <%= button_to '記録する', task_users_family_task_path %>
    </div>
  </div>
</div>
